如果控件有边框，那么左右边框会各占据2个字符宽度，而上下只会占据1个字符宽度
类内部变量负责保存状态（模型），paint 只负责绘制，inputproc负责处理用户交互，并把交互结果反应到模型中，这某某种意义上来讲是MVC模式

ATable水平方向和垂直方向翻页的策略不一样，水平方向是一个栏一个栏的翻页，而垂直方向则是凑足了一页才翻，因为垂直方向变化不大（每次一般之变两行），这样就比一行一翻能减少屏幕刷新（因为curses每次只刷新变化部分），但是水平方向无论是一个栏一个栏的翻页还是凑足了一页才翻网络消耗都是一样的，所以一栏一翻更好，也方便客户比较相邻的栏
代码写的越多，错误越多，bug越多

因为curses中中文占2个格，英文占1个格。所以不能采用unicode方式去进行字符串操作
业务开发人员要，这是天然支持的，只要窗口使用完毕以后不destroy掉，那么可以再次调用setvisible()将窗口显示出来了
暂时不做支持无限大屏幕的ScrollContainer，翻页效果由应用部门来做。如果以后应用部门有要求，则用Pad来实现，这样对整体架构修改也非常少，只要修改newwin为newpad并把show方法改一下就可以了。
要考虑wxpython的支持(在IDE层提供支持，在IDE中把代码写入UI模型中，这样只要针对wx和curses进行不同的代码生成就可以了)
应用开发人员只能调用控件的方法，不能直接访问属性，否则出现问题概不负责

国光的密码键盘的波特率为1200

在牡丹终端上运行异常的解决方法，执行如下的指令：
TERM=vt300
export TERM
然后运行就可以了
好像只在硬件上设置的终端类型不起作用，还必须修改软件

实达NT-560的配置界面：Shift+F2